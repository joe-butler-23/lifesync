import { List } from 'immutable';
import { CharacterMetadata, ContentBlock, EditorState } from 'draft-js';
interface ContentBlockConfig {
    key: string;
    text: string;
    depth: number;
    characterList: List<CharacterMetadata>;
}
export declare function getNewContentBlock(config: Partial<ContentBlockConfig>): ContentBlock;
export declare function getEmptyBlock(): ContentBlock;
export declare function getRootBlock(rootId: string): ContentBlock;
export declare function getEmptySlateState(rootId: string): EditorState;
export {};
