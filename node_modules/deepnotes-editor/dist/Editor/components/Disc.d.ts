/// <reference types="react" />
interface Props {
    collapsed: boolean;
    itemId: string;
    isCollapsible: boolean;
}
export default function Disc({ collapsed, itemId, isCollapsible }: Props): JSX.Element;
export {};
