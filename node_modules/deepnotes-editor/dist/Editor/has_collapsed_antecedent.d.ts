import { BlockMap } from 'draft-js';
/**
 * Find if parent or grandparent or some other ancestor in the tree is collapsed
 */
export declare function hasCollapsedAntecedent(blockMap: BlockMap, blockKey: string, zoomedInItemId?: string): boolean;
