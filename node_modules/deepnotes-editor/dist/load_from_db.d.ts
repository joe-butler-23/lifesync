import { RawDraftContentBlock } from 'draft-js';
interface Database {
    get: (key: string) => RawDraftContentBlock;
    findAll: (config: {
        where?: {
            [key: string]: string;
        };
    }) => Array<RawDraftContentBlock>;
}
export declare function loadFromDb(db: Database, startNodeKey: string): RawDraftContentBlock[];
export {};
