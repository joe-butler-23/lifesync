import { RawDraftContentBlock } from 'draft-js';
export declare function arrToObj<T extends {
    [key: string]: any;
}>(arr: Array<T>, key: string): {
    [key: string]: T;
};
interface Rows {
    [key: string]: RawDraftContentBlock;
}
export default class DB {
    rows: Rows;
    constructor(rows: Rows);
    get(rowKey: string): RawDraftContentBlock;
    findAll(options: {
        where?: {
            [key: string]: string;
        };
    }): RawDraftContentBlock[];
}
export {};
